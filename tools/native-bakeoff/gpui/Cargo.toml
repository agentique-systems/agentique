[package]
name = "agq-bakeoff-gpui"
version = "0.1.0"
edition = "2024"
publish = false
[workspace]
[dependencies]
gpui = { version = "=0.2.2", default-features = false, features = ["windows-manifest"] }

