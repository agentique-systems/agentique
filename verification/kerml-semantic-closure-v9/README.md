# KerML semantic closure v9

**KERML OPERATIONAL LIBRARY FOUNDATION INCOMPLETE — DO NOT PROCEED**

Branch: `semantics/kerml-reference-binding-errata-closure-v9`.
Starting commit: `62652e2841e4480d8067ebcc55b5a77a512f476a`.

V6 registers exactly the authorized five KERML11-145 corrections and the narrow
KERML11-8 reference-binding interpretation. Typed implied-binding provenance,
asymmetric result-endpoint validation, deterministic contextual-result structure,
profile identity checks and the seven-profile fixtures are implemented.

The subsequent authority review independently reproduces **KLCV9-F-001 /
KERML11-1** in two exact pinned Occurrences end-value declarations. This new
cross-feature selection/domain contradiction is the stop reason. No additional
correction is adopted, and complete v6 corpus expansion/publication is not claimed.

## Authority evidence

- [reference-binding-authority.json](reference-binding-authority.json) freezes
  exact formal rules, source identities, the ControlFunctions witness, default
  context derivation, truth table, current pilot construction/validator and XMI.
  KERML11-8 remains open. Only the expression-owned result endpoint qualifies.
- Direct FeatureChainExpression binding is excluded after separate review. A
  nested FeatureReferenceExpression retains its own semantic role.
- The [v8 five-rule matrix](../kerml-semantic-closure-v8/authority-matrix.json)
  remains the exact KERML11-145 authority. Separate producers implement each
  antecedent. The IndexExpression Array guard remains published; KERML11-69's
  Collection change is not adopted.
- [cross-feature-authority-conflict.json](cross-feature-authority-conflict.json)
  independently computes the two local end-typing closures and incompatible
  exact domains. It evaluates both the literal selector and the reading that
  excludes FeatureValue memberships. It also evaluates the literal Cartesian
  branch. No Agentique producer/query code participates in that Python proof.
- The pilot's extra cross-feature exclusions are corroboration only. All 36
  retained reference-XMI blob identities are independently matched to the newly
  acquired current release tree. Current pilot/release commits, acquisition dates,
  URLs, original bytes and SHA-256 values are in [acquisition.json](acquisition.json).

## Fresh measurements

The unchanged v5 production audit was started before the semantic edits. It exits
1 because semantic acceptance remains incomplete. Its exact command and full
output are in [baseline-quality](baseline-quality/result.json). Elapsed time is
recorded only for reproducibility, never correctness.

| Measurement | Observed |
| --- | ---: |
| Canonical records | 29,140 |
| Relationships included in those records | 16,695 |
| References | 4,000 |
| Unresolved references | 4 |
| Incomplete reference answers | 64 |
| Ambiguous candidate sets | 0 |
| Stored endpoint mismatches | 0 |
| EndFeatureMembership findings | 0 |
| Namespace distinguishability findings | 2 |
| Expression-result cardinality findings | 0 |
| Unevaluated executable elements | 3,901 |
| Named formal constraints | 258 |
| Retained current tracker issues | 410 |

[measurement.json](measurement.json) is generated by recounting the fresh report
and authority inputs. No historical count is a hard-coded expectation. These are
not presented as measurements of a completed v6 structural overlay.

The authority-applicability report retains all 258 formal-rule records. It records
the reviewed profile dispositions and the new positive conflict proof. Unreviewed
applicability remains explicit at this stop. The coverage gate is not closed, and
no missing structural rule is classified as DeferredExecution.

## Verification

Every capture records the actual command, output and exit code. The full matrix
also records durations and restores historical generated reports/screenshots.
Failed development attempts are preserved alongside their successful corrections.
[command-index.json](command-index.json) indexes these results and their output
digests; [closing-verification.json](closing-verification.json) records the final
verification outcome separately from semantic publication.

The focused [reference/result matrix](reference-result-matrices-final/result.json)
passes all five result-domain rules and seven profiles, ownership and inherited
referents, variable/shared/missing domains, missing/ambiguous source resolution,
nested expressions, authored and other-role implied connectors, a third endpoint,
and endpoint asymmetry. The independent cross-feature canonical witness runs
under all seven profiles. [Profile lineage](seven-profile-lineage/result.json)
checks the earliest correction boundaries. [Kernel overlay tests](kernel-extensions-1/result.json)
check immutability, order, evidence and invalid extension rejection.

[full-matrix-1/results.json](full-matrix-1/results.json) includes workspace tests,
generated metamodel checking, both structural runtime gates, strict Rustdoc with
`RUSTDOCFLAGS=-D warnings`, standards checks, frontend checks/build, Node/browser
tests, grammar checks, historical lexical corpus and independent runtime checks.
The initial Clippy run found fixture lint issues; [clippy-final-2](clippy-final-2/result.json)
and [format-final-2](format-final-2/result.json) retain the corrected successful runs.
The added lineage test is captured separately after the workspace matrix.

[product-1/results.json](product-1/results.json) covers the existing operational
product matrix, including official pilot validation, recovery and the headless
demo. [historical-authority/results.json](historical-authority/results.json) replays
the four historical authority verifiers and restores their output bytes.

Nonzero reports are interpreted separately:

| Report | Meaning |
| --- | --- |
| Baseline quality | Existing structural incompleteness; no semantic acceptance. |
| V9 coverage/publication status gate | New independently proven authority conflict and outstanding structural obligations; no accepted publication. |
| Raw KerML/SysML metamodel conformance | Published authoring anomalies, separate from the passing structural runtime gates. |
| Independent fixture validator | Existing Controller `RES001` finding for `r.count`; retained product regression evidence. |
| Legacy v5 preservation | Existing acquisition-index discrepancies; unchanged original bytes are separately checked against the starting Git commit. |
| Candidate binding manifest | Still stale; accepted regeneration remains conditional on successful semantic publication. |

[preservation.json](preservation.json) checks all 2,848 starting standards/evidence
files and all 18 new acquisitions. Neither historical evidence nor pinned library
source is corrected in place. The two complete structural runtime gates are
separate from language-semantic acceptance.

## Uncompleted gates

[gate-status.json](gate-status.json) records every requested gate. The full
feature-chain and positional audits remain unclosed. Renamed-member lookup,
incomplete reference support and Triggers inference remain ordinary implementation
work. They are not the stop reason. Complete corpus expansion, structural coverage
and authority applicability, strict semantic acceptance, accepted bindings,
`LoadedKermlStandardLibraries`, authored accepted-library integration and full
resource acceptance are not claimed. The ordinary five-rule/role fixture tests
do not establish those later gates.

The existing language quality operation remains fail-closed. `audits.py --gate`
is an explicit status check, not a newly finalized semantic acceptance operation.
Kernel storage validity and process completion never substitute for acceptance.

See [ADR 0019](../../docs/adr/0019-operational-reference-binding-correction.md),
the [new conflict review](../../docs/kerml-cross-feature-authority-conflict.md), and
the [22-question review](../../docs/kerml-standard-library-foundation-review.md).
SysML semantics, Systems Library publication, execution and application migration
remain outside this work.

## Offline reproduction

Run from the repository root; no acquisition occurs during these commands:

```text
python -B -X utf8 verification/kerml-semantic-closure-v9/authority.py --check
python -B -X utf8 verification/kerml-semantic-closure-v9/cross-feature-authority.py --check
python -B -X utf8 verification/kerml-semantic-closure-v9/measure.py --check
python -B -X utf8 verification/kerml-semantic-closure-v9/audits.py --check
python -B -X utf8 verification/kerml-semantic-closure-v9/audits.py --check --gate
python -B -X utf8 verification/kerml-semantic-closure-v9/preservation.py --check
cargo test --locked --offline -p agq-kerml-semantics --test reference_binding_v9 --test result_domains_v9 --test cross_feature_authority_v9
cargo test --locked --offline -p agq-kerml --test profile_lineage_v9
```

The status `--gate` command deliberately exits 1 while semantic acceptance is
false. Fresh command capture requires a new label; existing evidence directories
are never overwritten by `capture.py` or `run.mjs`.
