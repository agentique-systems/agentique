# Second actual native attempt

This attempt authenticated the accepted pair, restored the existing durable
Validated Agentique head, and delivered three real native screenshots. It did
not prepare or commit a candidate. Its exit code is 2 and the real journey failed.

The exact executable SHA-256 was
`8ccff768fc925c5cdef33b85d12eb66bde9583142079c0bb7eb8aafc7582b059`.
The process ran for 301.225 seconds with a sampled peak process working set of
5,448,941,568 bytes. These are whole-process observations, including runtime
authentication and project restoration; they are not scene or edit latency.
The source commit recorded at launch preceded the commit of the already-built
real-part focus eligibility fix. The executable hash, retained build command,
and integrated source history identify the actual build.

The redraw cadence correction worked: no diagnostic heartbeat or external
redraw request was used. The gallery retains the exact baseline manifest,
capture revision, selected object and image digest. The first port inspection
returned the actual `clientQueries` connection through `queryConnection` to
`platformQueries`, with a Complete effective query. This is real semantic
evidence, not a fixture relationship.

The next step failed because selecting ModelRepository from the filtered
Explorer had already entered it. The checker correctly refused to claim that
the selection remained inside ModelingPlatform. The Explorer had trusted the
toolkit's double-click count without checking the canonical identity of the
previous row. Filtering can reuse the same row geometry for a different
element. The correction applies the same object/generation/time/position guard
already used by the canvas. The native key helper sends both key-down and
key-up events and the Focus action lasts one input frame.

The screenshots also failed product review independently of that interaction
defect: the overview fit 38 scene nodes at approximately 23% zoom, ordinary
overview cards lost their labels, focus retained remote overview geometry, and
the Inspector exposed standard inherited features above project connections.
See the [independent image review](../reviews/real-system-first-light-review.md).

The next iteration uses one architecture level by default, separate bounded
layout memory for each world/focus, compact packing of differently sized root
definitions, readable overview titles and root-first Explorer/initial Inspector.
These are presentation changes. Canonical ownership, typing, inherited element
identity and the strict journey assertions remain intact. Their new screenshots
and real interaction results are still required before accepting the changes.

The failed run's database remains at the original unmodified Validated head.
The next run must use a fresh report/gallery and reauthenticate the runtime and
exact baseline/source identities through the strict resume preflight.
