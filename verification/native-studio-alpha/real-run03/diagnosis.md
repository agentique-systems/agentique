# Third real native run — retained failure and product iteration

The actual authenticated runtime reopened the exact durable Validated Agentique
baseline and completed 24 assertions through the contextual Create Part dialog.
Ten screenshots retain real projection DTOs, layout, revision identity, timing
and image hashes in their adjacent JSON files. No fixture substitutes for them.

The run failed during the first background preparation pan, before a candidate
response, validation or commit. The exact message is retained in `process.log`
and `journey.json`. Five input hooks were observed, with a maximum gap of 7 ms;
this does not establish a semantic UI freeze or responsiveness qualification.

The Prepare-release pass still renders its centered egui Window before closing
the dialog. The next raw input hook pressed at the same center. egui resolves a
press against previous-pass widgets and retains the drag target even when it
disappears. Source review identifies an obsolete dialog hit target as the likely
cause. The correction waits two completed UI passes after closure and sends an
actual bounded corner-canvas drag. Camera movement and the original 250 ms input
gap limit remain mandatory, including the settling interval.

The failed repository is preserved. A read-only audit independently found exactly
three ancestor revisions and their three commit receipts, an unchanged complete
head manifest, valid content addresses for all eleven blobs, and unchanged source
bytes. This is storage evidence, not semantic acceptance. The next run uses a
fresh repository through ordinary authentication and seeding; it does not reuse
an earlier report to bypass the failed-proposal resume guard.

System readability improved substantially: the initial real view displays seven
objects at 118% zoom, compared with 38 at 23% in run02. The formal review still
rejects the broad Graph, clipped Explain diagram and 8% default history diff.
These remain product defects to correct and capture again. The candidate and
separate durable restart gates remain unpassed.

Actual command, build/source identity, 601.864 s process duration and
5,852,807,168-byte process peak working set are retained in `process.json` and
`../checks/real-native-product-run03.{json,txt}`. The peak covers the whole run,
not an isolated candidate phase.
