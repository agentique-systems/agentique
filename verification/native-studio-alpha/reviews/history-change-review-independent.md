# Independent History change-review source audit

Reviewed exact held commit `27e2018adb4d98893089c98edd4e3f3981d139a8`, especially `crates/studio-native/src/history.rs`, against the real-run03 criticism that the full comparison fit 130 scene nodes at 8% zoom. This is read-only source review; no build, test, runtime query or new visual acceptance was performed.

Conditional source judgment: no new authority or mixed-revision action blocker found. `change_review` and `change_pair` require equal full ViewDefinitions and the scene's exact after revision. Every actionable target is resolved against its exact revision, retaining removed ghosts on their before revision. The app retains ordinary revision-bound selection/Inspector behavior. The single-slot summary cache includes scene generation, runtime epoch, binding, both revision identities and the full definition; the existing install path increments generation only when installing a complete scene. Project authority remains the upstream bound comparison contract, not a name or this presentation helper.

The summary compares exact projected node/relationship DTOs, normalizing only their outer revision identifier. It explicitly scopes counts to the view. Relationship groups use the source endpoint's recorded owner and say source owner context; missing source and missing owner are explicitly distinguished. This does not claim a connection's semantic owner or causal effect from its spatial placement. The local focus helper changes only the camera, preserving coordinates, ghosts, candidate lifecycle and binding. Its 0.42 zoom threshold is a presentation heuristic, not a measured readability result.

Ranked finding P2: in `diff_review_panel`, `selected_group.or_else(stored index)` overrides an explicit combo-box choice on every frame. If an operator chooses a group whose changes all have no scene target, the code cannot update the semantic selection, so the next frame snaps the chooser back to the old selected group. Such groups are legitimate: the retained exact comparison may include relationships omitted from the disposable System scene or objects outside the scene. Requested fix: remember the explicit group together with the selection identity, honoring it until semantic selection actually changes; add a focused state regression. Sent to the editor and integration lead before integration.

Remaining visual qualification: capture the actual historical comparison and candidate comparison after integration, confirm the default local focus is readable, exercise an invisible-target group, and verify that a removed ghost still opens evidence from its before revision. The real-run03 screenshot remains a failed product example; source-level safeguards do not replace the new image and interaction gate.

