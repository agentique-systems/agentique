# Exact CI failure diagnosis — 36194730967

Source: `4747f8eee085c939932d7be00abbf6af5a1c0ad2`. The native job passed; the main checks job failed. This review does not treat a passing native job as a passing workspace gate.

## Ranked findings

1. **Introduced Clippy regression, one-line correction prepared.** The actual workspace log rejects `crates/modeling-view/src/projection.rs:272` under `clippy::nonminimal_bool` and `-D warnings`. `git blame` attributes the opt-in profiling condition to `d404a8759891acc5d621dcad655fa4a981e17d63`. The correction uses `Option::is_none_or(|value| value != "1")` instead of negating `is_some_and(|value| value == "1")`. Absent environment variables and every value other than the exact OS string `1` still disable profiling; the exact value `1` still enables it. No query, DTO, timing boundary or authority changes. Source formatting is checked separately; workspace Clippy must run again after integration. The failed CI compilation may have stopped before reporting other warnings.

2. **Engineering-test failure is the expected freshness failure, not a second regression.** The engineering log reports 25 passed / 1 failed. The sole failed test is `SysML normative inputs and exact archive dependency closure verify offline`, at `tools/sysml-artifacts.test.mjs:27`. That test calls `verifySysmlArtifacts`, which calls `verifySystemsPublicationFreshness`; `standards:check` uses the same path. Both actual logs identify `crates/kerml-text/src/source_checkpoint.rs`: observed `8465c80bf7707c540399265d0447f5b0d44dba7730ff4ec3818a6f5e62010368`, retained input record `eb86d74ead07e68664081b131c3dbd152877fdb12c1374d3ba2175764a57aa0e`. Do not weaken this check or refresh its record before the separately required semantic-equivalence qualification. This patch changes no standards record or publication receipt.

3. **Independent fixture failure is pre-existing and remains a real nonzero gate.** `sysml-validate 0.43.1` reports `RES001` for the named transition payload `r` in `tests/fixtures/Controller.sysml`, line 21 in the editor (zero-based diagnostic line 20). The retrieved JSON is byte-identical to the file at inspected main `ded2a6e7dc616b9305508da2976d65282ebd7b4d`: SHA256 `99a52db50c41a7911047b8a5a824943f3dd9bbab8cfb4dae8da1d4c974660df5`. The command output SHA256 is also the retained historical `f95a8a36082010cd3b3e5bd29a7f2340c5168adaea35a4a1257cc8fa9a029714`. `tools/report.mjs` already records the disagreement with the official pilot, and CI intentionally retains the failure. This review neither alters the fixture nor converts the result to success. Generation-1 release completion remains separate from Native Studio progress.

## Actual acquisition and evidence

Artifact `verification-evidence`, id `10890283026`, belongs to run `36194730967` and the exact source commit above. Its API metadata reports 183,970,169 bytes and outer SHA256 `8b6bf9c33c7c931975be1f271606466987f5f20d0a21e6e5e17cba943e81b188`.

To avoid competing with the sole semantic oracle, acquisition used authenticated API resolution and HTTP Range reads of the ZIP central directory plus six selected entries. It transferred **593,320 bytes**, completed at **2026-09-25 22:33:05 UTC**, required a stable ETag, verified ZIP entry CRCs, and computed retained entry SHA256 values. The entire archive was not downloaded, so its outer digest is explicitly **not independently verified**. These are CI log records, not runtime-authentication evidence.

Exact small outputs, results and acquisition metadata are retained under [checks/ci-36194730967](../checks/ci-36194730967/retention.json). `retention.json` maps the executed ignored-directory files to their byte-identical committed copies. `read_artifact.py` is the exact acquisition script; it keeps credentials and the temporary signed download URL only in process memory. It refuses any individual transfer above 4 MiB. No cache or generated bulk was added.

The actual acquired command results remain unchanged: workspace Clippy 101; engineering tests 1; independent fixtures 1; standards 1. No Rust compilation, test execution or accepted-runtime consumer was run for this diagnosis. The prepared Clippy correction still requires the normal integration gate.
