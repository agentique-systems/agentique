# Real iteration four: exact dependency-view mismatch

The process exited 2 after 551.667 seconds, after eight successful real input
assertions. It authenticated the accepted runtime, created a fresh durable
Agentique repository, reached Validated with Complete semantic closure and
captured five real System/subsystem/port views. It did not prepare a candidate,
validate an edit or commit. The complete operator journey remains unqualified.

The failed ninth action requested the agent dependency view of ModelRepository.
The native request expected `ViewDefinition::semantic_graph()` with dependency
scope; the platform constructed the same scope with the name `Dependency
neighborhood`. The new full-definition response check rejected that mismatch,
retaining the previous projection. The ordinary query itself succeeded with
10 nodes and 13 edges. This was an integration defect between two constructors,
not a semantic or runtime authentication failure.

The repair shares `ViewDefinition::dependency_neighborhood` between the native
request and platform operation. The exact response check remains intact.
A native state regression executes the dependency command and accepts its exact
shared definition; separate negative cases still reject a changed scope, depth
or name. All ten view-intent regressions pass in the
[actual command receipt](../checks/native-dependency-lens-regression.json).

The [report](journey.json) contains no edit owner, candidate, mutation attempt or
background preparation input. Its final state has no pending request and retains
the exact Validated baseline. The existing guarded resume path may reopen that
repository, authenticate it again, verify its complete baseline manifest and
source identities, and repeat every journey action. No evidence is rewritten and
no passed assertion substitutes for the next actual run.

## Measured loaded-view delay

The opt-in profiler records real wall-clock phases without changing query DTOs.
ModelingPlatform projection took 5,582.540 ms: its two query-context constructions
took 2,662.513 and 2,654.371 ms (95.24% combined). The focused-interface queries
took 217.896 ms. Its Inspector took 5,573.838 ms, with two contexts totaling
5,306.898 ms. Query setup, rather than scene drawing, dominates these calls.
These observations motivate separately qualified call-local evaluator reuse;
they do not establish a speedup.

Whole-process peak working set was 6,262,132,736 bytes, including authentication,
seeding and native graphics. Browser/server compilation and end-to-end tests
overlapped bootstrap and completed before the retained modeling-view profiles.
This is not an isolated per-view allocation measurement.

## Visual review of the actual captures

Compared with iteration three, containment no longer carries redundant `owns`
labels, the selected Platform port remains visible at summary zoom, and the
Inspector reports eight parts separately from the modeled connection. The
inherited repository port retains its original canonical owner and an explicit
unknown direction. The five images support those bounded improvements.

ModelingPlatform still fits at 70%, with small child labels and substantial
space occupied by typing definitions. That is a remaining density concern.
Graph, Explain, History and Candidate improvements have no after-image in this
run and cannot yet receive visual acceptance.

The immutable [process record](process.json), [full output](process.log),
[command receipt](../checks/real-native-product-run04.json) and adjacent image
sidecars retain the executable, source, semantic identities and image hashes.
