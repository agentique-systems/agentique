# SysML Operational v2

Operational v2 preserves Published and Operational v1 behavior and adds only the
three authorized semantic target corrections: `ViewpointCheck`,
`viewpointChecks`, and `BinaryConnection`. The grammar remains the immutable
Operational v1 compatibility grammar; the new semantic correction manifest is a
separate context-identity input.

Focused authority, syntax, publication-gate, transition-accepter and
cross-language variable-feature invalidation regressions passed. This evidence
does not accept a Systems Library publication: the candidate still lacks the
sound producer-family closure witness required for negative effective-owner
typing.

See `authority-decision.json` and `summary.json` for the scoped authority and
actual command outcomes.
