# Medium uninterrupted/resumed acceptance prerequisite

**PASS.** `medium-exact-equivalence` exited 0. Exact reports and authenticated
artifact identities are recorded in `medium-equivalence.json`; the actual
commands, outputs, durations and exits are in `commands.json` and
`publication-commands.json`.

Both paths parsed and constructed 13/13 exact documents, audited 695/695
mandatory references Complete with every failure category zero, retained zero
kernel obligations, and converged Complete with all 14,791 producer pairs and
417,786 requirements closed. Incremental certificate maintenance was checked
against the full rebuild at every newly computed frontier.

The repaired authenticated resume completed in 1,180.25 seconds, restoring four
invocations (three completed) and skipping 60 completed rounds. It continued an
unfinished ContextualBindings frontier. The independent uninterrupted run
completed in 2,389.688 seconds with no restored invocation or watchdog stop.
The earlier disk interruption and handoff assertion failure remain recorded;
neither is counted as a successful uninterrupted run.

Exact identity fields and model, semantic-closure, certificate, context-contract
and registry digests match the SHA-256-pinned historical medium report. The two
new runs additionally match mandatory-query result and certificate-revalidation
digests. Independently authenticated final journals, archives and decoded state
bind 4,252 selected ordered-reference contributions to identical normalized
proof/search evidence, digest
`cacc79b48c4dd4c27041accb9db39bdac1c34ddcb403f3326b28776be022adfd`.
The historical report has no selected-contribution archive; that additional
comparison is between the two new runs, not a claim about missing old evidence.

The executable is pinned in `publication-executable-final.json`. Its retained
bytes equal the binary used for both successful medium runs. Disposable Cargo
release intermediates were cleaned only after the running process exited and
the retained copy's digest was verified. No source, original library, accepted
cache, checkpoint or evidence artifact was removed.

This passes the prerequisite for the one full monolithic Systems attempt. It
does not accept the complete Systems Library, activate trusted authority, adopt
ADR 0026 or integrate the production modeling workspace. ADR 0027 remains
proposed research.
