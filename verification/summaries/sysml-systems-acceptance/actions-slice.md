# Actions corpus preflight

The first bounded run used exact `Actions,Connections,Constraints,Flows,Items,Parts,Ports,States`
sources and the trusted accepted KerML v9 cache at implementation `ea715cf`.
The actual command, source identity, output hash, exit code and resource readings
are recorded in `commands.json`; raw output remains ignored.

Preparation exited 1 after 154.406 seconds with
`Derivation(InputContextMismatch)`, following structural producer frontier 6.
Peak private memory was 4,266.9 MiB; no workflow limit stopped the process.
Reference refinement provisionally selected 532 of 534 endpoints and retained two
kernel obligations. The final mandatory-reference audit was not reached, so these
numbers are not Complete reference results. The last frontier retained three
formal negative-type diagnostics and 24 producer-closure diagnostics.

The same rejection was reproduced by a directed, non-valuation FeatureValue:
its structural phase published an unowned contextual feature, then its binding
phase attempted an undeclared ownership change. The corrected producer creates
that helper atomically with its binding. The directed combined SysML regression
passes, and generic scheduling tests preserve the final one-shot graph/provenance.

A nested State/Transition reproduction also exposed an overbroad source-feature
search. The pinned query excludes FeatureMembership subtypes; the generic search
contract and closure matcher now represent this excluded population explicitly.
Native namespace/import structure searches now retain their precise scope during
canonical proof persistence. Filtered ownership queries also retain original
declared support when every selected reference belongs to the declared prefix,
avoiding unrelated appended-member provenance. Broad and mixed derived reads
retain their full proof, including when they follow a declared-only observation.
The nested State/Transition regression and all 53 SysML tests now pass. The
generic matrix passes 41 tests; release compilation precedes the next bounded run.

The integrated KerML semantics package passed 280 tests (two existing scale
probes ignored) at `18d5147`, after updating the context-rejection assertion to
check the new exact `formal_constraint_targets` diagnostic. The corresponding
command and output identity are in the producer-closure summary. A separate real
additive-ownership regression established and repaired missing Ownership effects
in all six closure requirement masks; its generic matrix passed 38 tests.

Neither issue is a grammar or normative authority conflict. No medium or full
Systems publication attempt has run; the full-publication attempt budget is unused.
