# Selected transition payload chain evidence

Payload-specialization redundancy uses exhaustive ancestry only to discover
candidate IDs. A complete matching chain must also have a checked canonical
specialization witness from the input parameter. Only that selected path and
chain join the independently required trigger/input antecedents. No witness
does not prove absence; the existing deterministic proposal remains available.

The regression constructs the real payload chain plus an unrelated derived
ancestor. The exhaustive ancestor proof contains that unrelated premise, while
the repeated payload result excludes it, remains Complete and suppresses the
already materialized chain. Trigger-before-payload ordering and deterministic
output IDs remain asserted.

- `cargo test -p agq-sysml-semantics --lib transition_tests -- --nocapture`:
  exit 0; 6 passed, 0.67 s.
- `cargo clippy -p agq-sysml-semantics --all-targets -- -D warnings`: exit 0.
- `cargo fmt --all -- --check`: exit 0.

The broader receiver fixture is still Incomplete. Its trigger parameter query
also imports unrelated ownership append proofs when a selected relationship
was derived; exact kernel contribution evidence is being handled separately.
No publication acceptance is claimed here.
