# Independent selected contribution review

**GO for `b6bcfaa` + `fa3bbdd` + `4fa87cd`**, reviewed together at `17c56c7` with independent test corrections `dc888fd` and `7adca6b`. This decision covers exact ordered-reference evidence, not Systems publication acceptance.

The kernel captures an appended reference's explicit attachment proof before merging prior aggregate or sibling evidence. For references created with a derived element, selected support retains that element's actual creation proof, the selected target, and both record and slot searches. A declared relationship's existence alone never substitutes for its later attachment guard. Typed queries still retain their current population searches and therefore their provider obligations. An actual broad query retains the aggregate evidence in either merge order.

Repeated target positions cannot be represented by one owner/property/target key and use aggregate evidence. Later unattributed search updates discard cached precision. Archive bytes remain unchanged; restored local contributions have no precision metadata and use aggregate evidence, while supplied immutable dependency metadata remains shared. Checkpoint fingerprints reopen readers when contribution support or its availability changes. Loss of this optional index does not itself alter canonical graph identity.

Independent regressions cover ordinary and compact producer queries; derived creation and declared adoption; mixed original/appended populations; changed attachment guards with identical owner records; archive fallback and reconstruction; initial derived ownership; and both orders of mixed broad/narrow evidence. Compact checks follow retained canonical dependencies into their kernel explanations, rather than requiring expansion into declared leaves.

Final verification on the complete follow-up:

- Kernel `reference_contributions`: 7 passed.
- KerML `selected_contribution`: 5 passed.
- KerML `producer_`: 97 passed, 2 existing ignored scale probes.
- KerML `publication_overlay::restoration::tests`: 5 passed.
- `cargo fmt --all -- --check`: exit 0, no output.

[The compact command summary](independent-selected-contribution-review.json) records commands, source identities, exit codes, durations, and output hashes, including earlier unsuccessful checks. Those exposed a temporary module-hook merge conflict, incomplete synthetic fixture evaluation state, compact-evidence assertion assumptions, and three assertions tied to aggregate ownership evidence. The final tests preserve selected guards and broad-read requirements under the new representation; no production behavior was changed to satisfy them.

The receiver microfixture and corpus publication belong to the integration run. No corpus command was run for this independent review, and no trusted catalogue entry or accepted publication was created.
