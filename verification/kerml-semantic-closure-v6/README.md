# KerML semantic closure v6

**KERML OPERATIONAL LIBRARY FOUNDATION INCOMPLETE — DO NOT PROCEED**

The new stop is independently reproduced **KLCV6-F-001 / KERML11-205**,
[documented here](../../docs/kerml-subobject-specialization-authority-conflict.md).
Its published subobject-specialization target conflicts with the prose and the
actual pinned library. The witness's antecedent uses explicit source facts;
the prose requirement is reflexively satisfied while the formal target is absent.
This is not an incomplete inheritance/order implementation presented as authority.
No correction for this issue is applied. Ordinary structural work remains
mandatory after that authority decision.

## Implemented and independently checked

- Explicit `agentique-kerml-1.0-operational/4`, extending frozen v3 with only the
  authorized KERML11-68 validation semantics. V3 canonical correction IDs and
  origins are preserved. The default operational profile remains v2.
- The actual official Git object and parent diff for
  `d9231d21e621aeabeafa92aef026b3929c859116`, all before/after validator and test
  blobs, the open issue, formal/preliminary clauses and current implied XMI.
  See [authority-packet.json](authority-packet.json). The reference Xtend test
  changes were inspected and pinned; a new full pilot Maven/Xpect build was not run.
- The canonical 220-case end/owner/profile matrix, including Association and
  Connector subtypes, missing owningType, nested features, inherited ownership,
  correction-origin facts and unresolved endpoints. Published through v3 keep
  their historical end rule. V4 changes validation disposition only.
- Ordered owned-specialization projections, canonical inherited results,
  positional result redefinition, terminal feature-chain end populations,
  and name agreement with typed ambiguity when implied targets disagree.
  No ID, hash, declaration creation order or arbitrary first target establishes
  implied naming order. Complete corpus closure and independent positional target
  comparison are not claimed.
- Additional local classifier, subsetting, parameter ownership/direction and end
  membership constraints, with focused valid/invalid fixtures.
- Authored profile selection across five profiles, and exact full-corpus v3/v4
  canonical record/source-map equality. These are not accepted-library consumption.

[ADR 0016](../../docs/adr/0016-operational-semantic-validation-corrections.md)
and the appended [foundation review](../../docs/kerml-standard-library-foundation-review.md)
retain the acceptance boundary and answer every required review question.

## Measured corpus status

[quality-comparison.json](quality-comparison.json) groups the complete fresh
baseline and final-code audit, retaining each finding and its document/subject.
[validation-coverage.json](validation-coverage.json) retains every named formal
constraint and actual explicit-check executions. It is **not** a passing coverage
gate; unevaluated structural constraints are never counted as successful.

[reference-positional-oracle.json](reference-positional-oracle.json) independently
captures all available implied reference redefinitions selected by result/end/
directed-parameter facts, with actual XML ownership positions and target identities.
Its Agentique matches remain explicitly null: this is comparison input, not a
completed differential check or authority for implied insertion order.

The fresh baseline uses the new v4 profile with the prior ordinary query rules:
29,140 records, 4,000 references, zero unresolved/ambiguous/mismatched endpoints,
453 incomplete reference answers, six distinguishability findings, 3,496 result
findings and 3,901 unevaluated expression/function elements. These are measured
values in `quality-baseline-v4-release/quality.json`, not expected final counts.
The final measurements come from `quality-order-results-v4-3/quality.json` and
must be read separately. Its query-rule identity is `agq-kerml-query/14`.

| Measurement | Fresh baseline | Final audit |
| --- | ---: | ---: |
| Canonical records | 29,140 | 29,140 |
| References | 4,000 | 4,000 |
| Unresolved references | 0 | 4 |
| Incomplete reference answers | 453 | 60 |
| Ambiguous candidate sets / stored endpoint mismatches | 0 / 0 | 0 / 0 |
| Namespace distinguishability findings | 6 | 2 |
| Expression-result cardinality findings | 3,496 | 0 |
| EndFeatureMembership end-flag findings | Not checked | 180 |
| Unevaluated executable/function elements | 3,901 | 3,901 |

Zero observed result-cardinality findings does not establish complete expression
structural semantics. The formal inventory contains 258 constraints and 38
explicit checks; its mandatory structural coverage gate remains incomplete.

[individual-findings.json](individual-findings.json) classifies the 215 emitted
diagnostics as 180 category A construction defects and 35 category B structural
inference/resolution findings. The four unresolved references are in Occurrences
and require redefinition search across renamed inherited members. The implied-name
ambiguities in Clocks and VectorFunctions remain category B pending complete
implied-specialization closure; they are not asserted to be genuinely unordered
source cases. Both distinguishability findings are in Triggers. The 180 end
membership failures expose default false end flags in lowering. None is an
authority stop or an execution deferral. Diagnostic counts and incomplete query
counts measure different populations and are not interchangeable.

The full semantic acceptance operation, complete implied expansion, accepted
immutable facade, accepted bindings, authored accepted-library integration and
accepted-publication stress suite remain unissued. No source or model correction
entry was added to conceal a structural finding. Execution remains separately
deferred. Systems Library publication and SysML semantics remain out of scope.

## Authority reproduction

```text
python -B -X utf8 verification/kerml-semantic-closure-v6/verify-authority.py --check
python -B -X utf8 verification/kerml-semantic-closure-v6/verify-subobject-authority.py --check
cargo test --locked --offline -p agq-kerml-semantics --test redefinition_end_v4 -- --nocapture
cargo test --locked --offline -p agq-kerml-semantics --test subobject_authority -- --nocapture
```

`verify-authority.py --git` additionally verifies the objects in the explicitly
acquired local bare repository `.workspaces/kerml-v6-reference.git`. Ordinary
offline reproduction uses the retained raw Git object, exact diff, API metadata
and immutable blobs. `acquire.py` is explicit maintenance, never a build step.

The subobject verifier independently reads ZIP and XML, checks all 36 pinned
library package names, exact PDF/XMI clauses, reference facts and five complete
arbitrary-name canonical target searches. It retains the exact OCL text;
it does not claim to execute that OCL as a program. Its conflicting required
target is evaluated under the separately documented semantic antecedent.

## Actual command evidence

`capture.py` writes command arrays, output, timestamps, exit codes and input hashes
to a fresh directory. `run.mjs` captures the full existing matrix and preserves
the original product-generated evidence bytes. [command-index.json](command-index.json)
and [closing-verification.json](closing-verification.json) index the results.

- `complete-matrix-1`: workspace tests, generated descriptors, both complete
  structural runtime gates, strict Rustdoc, standards integrity, frontend check/
  build, Node tests, browser tests, grammar checks and independent runtime audit.
- `rust-regression-final`: complete workspace tests after the ordering and bounded
  refinement changes. `clippy-final-2` and `format-final` contain the corrected
  warning-free and formatting results. `ordinary-validation-matrix` checks the
  subsequently added valid/invalid test fixtures.
- `product-regression`: existing product release checks, official pilot wrapper,
  process recovery and headless demo. The historical external Controller fixture
  `RES001` remains a separately failing baseline diagnostic. The first workspace
  build collided with the running E2E server's Windows executable lock;
  `workspace-build-final` passes after E2E completion.
- `published-conformance`: strict published raw metamodel conformance remains
  separately failing. Neither that audit nor kernel storage is semantic publication.
- `preservation-final`: all 1,692 files pinned at this turn's start are unchanged,
  and all v6 acquisitions match their captured byte hashes. `preservation.json`
  records nine **pre-existing** v5 acquisition-index discrepancies, independently
  checked against committed `main`. The original files and old index are retained.
  Two discrepancies correspond exactly to CRLF normalization; no cause is asserted
  for the others. `.gitattributes` now protects v5/v6 captures and v3/v4 manifests
  from further text normalization.

Failed and superseded attempts remain present: the initial end fixture used a
scalar for ordered ownership; a focused regression found over-eager owningType
inspection and was corrected; Clippy caught an unused import and a redundant
clone in a test. The debug corpus run was canceled in favor of optimized code.
`quality-order-results-v4-1` was canceled after an ordering projection correction;
`quality-order-results-v4-2` was canceled at approximately 5 GB memory before
adding 128-reference proof batches. These canceled runs supply no final counts.
The final audit evaluates the same immutable context across every batch and keeps
all semantic obligations. No timing threshold establishes correctness.

The initial worktree was clean, `main` was fetched and fast-forward checked, and
the branch is `semantics/kerml-validation-errata-semantic-closure-v6`.
See [preflight.json](preflight.json). No accepted publication, bindings, deployment
or external communication is performed by these scripts.
