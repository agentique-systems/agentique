# Independent review of real journey and bootstrap changes

Reviewed the unstaged native changes over `69287bdb9e0ad838d6355d981bf5b9ce7131edac` before the `real-run02` execution. This is a source review, not a claim that the new operator journey passed. No production changes or builds were performed during this review.

No critical source/revision-binding flaw was found in the new launch, requirement neighborhood or History assertions.

* Fresh real acceptance explicitly opts into importing the self-model. `seed_isolated_project` requires the real scenario, excludes fixtures/resume/restart, and reruns the launch validator before worker creation. Validation requires the explicit absolute new database, absent database and presentation sidecars, preserved prior report/gallery and a deadline. The platform policy still requires an empty repository for this opt-in. Ordinary generic project opens keep the flag false; existing generic projects remain protected even with the flag true. Existing Agentique journals retain their previous resume behavior.
* The requirement check preserves the authored `ImmutableRevisions` identity, exact two-hop `SubjectMembership`/`FeatureTyping` path, focus, selection and Inspector revision. It now permits a focused count equal to the overview when the real self-model already contains only that minimal three-node neighborhood. The test rejects disconnected/out-of-radius nodes and incorrect depth. Counts remain observations, not evidence that modeling obligations were satisfied.
* History enters through the ordinary keyboard action, clicks the actual parent revision card, then verifies the exact requested durable manifest, project/revision binding, projection revision and unchanged branch head. Returning through the ordinary head button compares the saved world, complete view definition, camera, selection/revision, branch, collapsed/expanded state and panel visibility. The second visit checks the changed AgentRuntime Graph context. Exact observed and requested manifests are retained separately, including on failure.
* `project_dialog.rs` is a test-module relocation after production declarations for Clippy; no project-creation behavior changed in that diff.

One bounded target-recording concern was sent to the interaction agent and integration lead: History rows are allocated inside a ScrollArea, but their real-input target records the entire rectangle. `real_targets::target` limits geometry age to two frames and requires viewport containment, but does not check the ScrollArea clip. A row clipped above the scroll region can still be inside the window viewport and could yield geometry over a toolbar. The immediate parent should normally be visible in this particular two-revision journey; exact post-click manifest assertions prevent a false successful parent visit. Clip-aware recording is advisable before claiming arbitrary scrolled revision selection coverage.

Remaining qualification depends on the real run: the source assertions do not establish readable revision summaries, retained apparent node placement, satisfaction/verification coverage, or correct behavior on actual asynchronous timing. Requirements architecture-first navigation and Diff mode/change-navigation controls remain explicit capture gaps listed in `redteam-capture-coverage.md`.
