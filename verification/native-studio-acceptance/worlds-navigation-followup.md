# History return and summary follow-up

Return to branch head invokes return_to_revision(), supplied by the interaction
follow-up, so an existing non-History visit restores its full presentation. This
change depends on that interaction commit for compilation. Generic revision cards
also distinguish source deletion from an empty initial Working project, and the
Requirements outliner names the neighborhood it actually displays.

Ran cargo fmt --manifest-path crates/studio-native/Cargo.toml --all and git diff
--check; both exit 0 with no output. Compiled tests remain pending root integration.
