# Alpha product review ledger

**Current judgment: not yet a confident Alpha demonstration.** This ledger records five reviewer perspectives on the actual real-run02 gallery. They are five reviews within one image-backed review/change cycle, not five completed post-fix critique/change/recapture rounds. The new capture is pending. No fixture or source test is counted as a new real-product review.

The reviewed executable is `fb2fedafc3d2e1574f526d1763cc99521cce64b4e7ad058e29665d616004e30e`. The real self-model gallery is retained at `verification/generated/native-studio-acceptance/real-run02/gallery`. The completed first process has 39 passed journey assertions and exit 0; its outcome is `journey_passed_restart_pending`. Restart and source-only recovery evidence belong to separate gates.

| Review perspective | Largest criticism in actual images | Bounded change resulting from review | Remaining weakness and evidence status |
| --- | --- | --- | --- |
| Systems engineer | In `04a-requirement-neighborhood`, the known `ReferenceUsage` subject and `ConstraintUsage` owned constraint are both shown as `ELEMENT` with question-mark Explorer icons. This obscures the obligation's engineering structure. | Exact kind mappings distinguish Reference and Constraint in canvas, Explorer and Inspector. Unknown kinds remain unknown; no suffix inference or satisfaction claim. | Source change is prepared; integrated scene tests and new capture pending. Real evidence has one obligation, four nodes, zero satisfaction and verification declarations. Architecture-first inquiry and richer declaration populations need additional real evidence. |
| Product designer | `06a-earlier-revision` orders baseline, empty project and newest revision by UUID rather than ancestry. `06-history-diff` opens cropped and retains an unrelated requirement Inspector. `10-committed-history` exposes raw edit-receipt JSON below its useful intent title. | Commit `fea3339d` orders by parent lineage with branch-aware deterministic ties, initializes durable Diff with relevant selection and changed-owner framing, and labels structured receipts as evidence available on hover. | Linux native tests/Clippy and macOS compilation passed for this commit. Updated real images pending; source import summaries remain less meaningful than authored intent. Dense comparisons may still require focus and progressive detail. |
| Graph expert | The actual agent result fits ten semantic results at 46%, leaving several names truncated. Dense parent Diff paths remain hard to trace. Reviewing the missing twenty-parallel-route gate exposed an additional selection defect: the lowest-ID neighboring route could win over the visible line under the pointer. | Nearest segment distance now breaks ties within existing port/node/edge precedence. A twenty-route regression probes exact relationship identity at five zooms, three pointer offsets and reversed input order. | Source and test are prepared, execution pending. This corrects inspection trust, not the remaining broad layout problem. Dedicated dense bipartite and many-inherited-relationship native reviews remain open. |
| Editor interaction expert | In `08-candidate`, selecting the newly created part identifies it in Explorer/Inspector but leaves it offscreen at the inherited 120% camera. | Commit `fea3339d` reveals offscreen Explorer selections by panning while preserving zoom where possible; visible selections retain camera. Real automation now requires selected scene bounds to intersect the viewport after ordinary settling. | Linux native gates and macOS build passed; new actual capture pending. Preparation still took 171.512 seconds. The current revision was interactive during it, but responsiveness does not make the wait acceptable. |
| AI-native workflow expert | `07-agent-view` repeats a Graph recommendation when already in Graph and devotes substantial Inspector space to it. The result is harder to read than its bounded size suggests. | Review retained the bounded semantic dependency operation and its explicit provider, input revision, intent, subject and result provenance. No general provider or new World was added. Selection visibility and exact semantic labels address adjacent trust problems. | Recommendation chrome and compact result fitting remain unresolved; no reduction is claimed. The deterministic recommendation stays labeled as a mock. Explain's actual derivation remains explicitly partial. |

Detailed observations and image hashes are retained in [systems/product review](../redteam-real-run02-worlds.md), [graph review](../redteam-real-run02-graph.md), [editor/agent review](real-run02-editor-ai.md), and [image/sidecar evidence](real-run02-editor-ai-evidence.json). The requirement-focused image before History and restored image after Return to head are byte-identical (`5ff6c560bb330f7d1f4ee97307497c43d174eab517d3c4a3ee48ff447873ba16`). Two actual historical visits preserve the live branch head and restore the corresponding Requirements/Graph exploration contexts.

## Qualification of the first image fixes

For exact source `fea3339dbf0e84d408cb7d47c802a7b047826f1f`, GitHub Actions run `36234506697` reports the [Linux native job](https://github.com/agentique-systems/agentique/actions/runs/36234506697/job/108383732132) successful, including native format, Clippy and tests, and the [macOS native build](https://github.com/agentique-systems/agentique/actions/runs/36234506697/job/108383732348) successful. These are build/test qualification, not interactive Linux/macOS qualification. The overall workflow was still running when reviewed. The nearest-edge and category follow-ups are later unstaged changes and are not covered by that commit's results.

The initial durable Diff implementation preserves an explicit candidate selection: its initializer returns immediately when a candidate exists. It acts only on the initial durable comparison, preserves an already relevant changed target, and frames all available changed-owner headers after actual viewport dimensions are known. Canonical scene contents and historical Return-to-head restoration are not rewritten. Regressions cover non-chronological UUIDs, branching lineage, initial unrelated selection, unchanged semantic scene contents, and candidate ghost selection.

## Next actual recapture obligations

The same real journey must show the corrected lineage and structured receipt disclosure, an initially useful durable Diff with relevant Inspector, a visibly selected new candidate part, and intentionally named Reference/Constraint cards. It must again visit the earlier revision through a real card and restore exact saved Requirements and Graph exploration state. The review should retain crowded Diff and agent scenes even if still unflattering. Each of the five perspectives should then record what visibly improved and what remains; only those observations complete another review cycle.

Passing these visual follow-ups cannot establish Alpha alone. The current separate-process exact semantic oracle proves equivalence but records 219.531 seconds for the golden edit against 163.268 seconds for current cold reconstruction and 161.497 seconds for the prior command path on the same CI runner. True incremental wall-time improvement is not established. Warm restoration, cancellation/soak, durable restart and source-only recovery remain independently assessed acceptance obligations.
