# Actual product review round 2: corrected Native Studio

**The corrected journey is more credible, but the initial durable Diff still fails as a readable explanation of design changes.** This is the second actual image-backed review round: all 16 new real-run03 screenshots were inspected, including crowded and unsuccessful presentation states. It is not five rounds inferred from five reviewer roles. Source was `42e7e187de823a3b855d530dcee84dbe8c06b59f`; executable SHA-256 was `c2d023c62b80c89791a6a1d4ea11626b86b46c87b51812dedd2034ccefddfa5a`.

The process exited 0 after 540.194 seconds and all 39 assertions passed. Its explicit outcome remains `journey_passed_restart_pending`. This visual review makes no incremental-speed, restart, soak or overall Alpha claim. Source startup and candidate timing must be assessed from their actual measurement boundaries separately.

| Perspective | Actual change since round 1 | Largest remaining criticism | Next bounded change or evidence |
| --- | --- | --- | --- |
| Systems engineer | `04`/`04a` now show REFERENCE for subjectWorkspace and CONSTRAINT for preservesPriorState, with intentional Explorer icons. The requirement, subject, typed system and owned constraint are legible. | The self-model still exercises only one obligation and no satisfaction or verification declarations. Identifier-only constraint detail does not explain a full obligation statement. | Retain the honest zero counts and no-success claim. Broader semantic-role qualification needs real declarations; do not invent fulfillment status or expand the model just for a flattering screenshot. |
| Product designer | `06a` orders current head, selected baseline, then empty project by actual lineage. `10` places the new authored intent first and replaces inline JSON with recorded-evidence disclosure. `06` now inspects AgentArchitecture rather than unrelated ImmutableRevisions. | Initial durable Diff fits dispersed owners at approximately 25%, truncates most names and clips the scene. Its owner chooser says Owner outside this view while the Inspector shows AgentArchitecture. | Enter a bounded neighborhood of a real present changed owner; synchronize initial owner choice, selection and Inspector. Keep the five-owner summary and explicit entire-comparison view. |
| Graph expert | The six-node Repository neighborhood remains traceable with incident-path emphasis and exact typing/specialization labels. Candidate Diff at 70% exposes the added part and changed owner together. | `06` shows only 62 of 130 scene elements and a band of tiny cards despite extensive unused vertical space. `07` still fits ten semantic results into a wide 46% strip with truncated names. | Prioritize the durable comparison entry defect. Nearest-edge correctness has a regression gate, but these static images cannot independently verify its pointer behavior. Dense bipartite/inherited native qualification remains open. |
| Editor interaction expert | `08` now places selected alphaStudioObserver fully onscreen at the retained 120% zoom; Explorer, canvas and Inspector agree. `09` retains the same selected candidate through Diff. Both actual historical visits return to the corresponding saved exploration context. | Candidate view's status text still says nearby context for 0 of 1 visible changes after the selected part becomes visible. The text describes an earlier framing operation and is stale as a visible-state claim. | Preserve candidate-selection and historical restoration contracts while correcting durable entry. Clear or refresh operation-specific framing status after subsequent camera/selection operations in a separately scoped follow-up. |
| AI-native workflow expert | `07` still identifies the built-in provider, dependency intent, ModelRepository subject, input revision, completeness and temporary read-only result. Explain continues to label its shown derivation as partial. | The already-applied Graph recommendation occupies a large repeated panel above the selected object's engineering information. | Collapse the already-applied recommendation into a compact disclosure, retaining the deterministic-mock label and provenance on expansion. This is a smaller priority than the durable Diff defect. |

## Largest remaining visual defect and precise correction

The prior initial-Diff fix resolved unrelated Inspector state, but fitting every widely separated changed owner did not produce a useful overview. `06-history-diff` has a semantic projection of 159 nodes/425 edges and a scene of 130 nodes/405 edges. Its sidecar camera zoom is 0.256661, matching the tiny-label capture. The visible selected AgentArchitecture belongs to an enclosing namespace outside the view, so preferring its change-membership group makes the chooser display Owner outside this view even though the same element is itself a useful present owner of changed children.

The bounded fix should prefer a present owner matching the selected canonical object before that object's enclosing absent-owner group. With an unrelated prior selection, choose a deterministic present group with visible structural changes. Frame its existing local neighborhood at readable scale; do not union distant owners into initial camera bounds. If no group has a present owner, use a real visible changed object without fabricating ownership. Keep all semantic comparison data, modes, group counts and explicit Fit entire comparison. Candidate review and saved History return must retain their existing camera/selection behavior.

Regressions should include an added package whose enclosing owner is absent while the package owns changed children, several far-separated present owners, all-absent owners with visible changes, explicit candidate selection, and invariant comparison contents. A fresh real capture is required before declaring this defect fixed. The current durable Diff remains Foundation; History and the bounded Requirements/candidate presentation now support Alpha-quality visual judgments in the demonstrated scope.

## Retained evidence

[Round 2 evidence](real-run03-round02-evidence.json) records the exact process, journey, image and sidecar hashes and compact semantic observations. The before/after Requirements exploration images are byte-identical: `b77890708578b802c01cd1b01ec99832113cc2b0e96be86fd7dd49b2164601eb`. The journey records two actual parent visits with requested/observed manifest equality, parent-bound projections and return to the prior live revision. The candidate and candidate-Diff sidecars retain candidate `a828f853-3043-43b0-a656-b79ac2bee54f` and selected part `4029aa1a-e38e-54f6-b75b-384609c506d0`; their current branch binding remains `cce9510f-dab3-467e-944e-45b8c38afa8b` until commit.

Review performed by Codex `/root/worlds` using actual PNGs and accompanying semantic evidence. No production change was included in this review round. Proposed changes require their own tests and recapture.
