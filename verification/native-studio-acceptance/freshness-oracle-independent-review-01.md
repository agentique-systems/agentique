# Independent exact oracle and freshness proposal review

**Correctness evidence passes for source `a6e1f41d2b549a8be10a79dfa3ec786c090b65b3`; performance acceptance does not. The emitted freshness patch is review-only and has not been applied.** A subsequent semantic production change invalidates its source binding and requires new review/proof.

The integration lead has now introduced a new shared-dependency checkpoint optimization in production. Consequently proposal01 is historical evidence for the earlier source and must not be applied to the changed tree. Its immutable proof set and patch remain retained for review; they are not silently refreshed.

The actual [Windows baseline/current oracle job](https://github.com/agentique-systems/agentique/actions/runs/36231357374/job/108375005393) completed successfully. Both paths used the same runner, pinned Rust 1.92.0 Windows MSVC target, checked-in release profile and exact accepted runtime transport SHA-256 `37edf34cc0220ecdded8e0162f3fc1955ee2f3849e6dccf7ba373833ade3b026`. Current source was clean before and after compilation. Baseline used source `bd72aee4e1bf2aa2b894dc0201bcef111ddf337b` with the same portable oracle harness overlaid; it is a timing/control reference, not qualification of current production.

I independently hashed the downloaded binaries, harnesses and raw execution logs and compared them with the build/invocation/observer receipts. Current executable SHA-256 is `6c0ffa4cf472c2067491fe329691c10fa6993b855cc444d503ea191d9f8bca0c`, harness SHA-256 is `7fdde70821f09f347df3051b3b1643fdbdbe1a6c13789d73edde786e17b6814f`, and current raw output SHA-256 is `31a55f1771966bc6bba0668d09fde381b621e406986638fc79b982eb108c1dc4`.

The current and baseline each retain two complete observation maps, one from the command child and one from the independently mounted cold child. Within each pair, all 697,419 keys and values compare equal, not merely their counts. The maps cover 76,153 canonical elements, 572,918 declared slots, 363 relationship occurrences, 6,477 contributions, 39,783 computation-search entries, 85 references, 1,633 applicable local effective query results, source checkpoint, semantic fingerprint/closure, strict-audit report/subjects, diagnostics and the created part identity. The harness normalizes only the newly allocated kernel revision label within Debug observations. Query observations include evidence and completeness; the cold child restores authoritative checkpoint/source inputs, reparses all six authored documents, reports no project semantic cache use and independently validates.

The retained current log records all sixteen malformed reuse cases rejected. The command and cold child each report one test passed, zero failed/ignored, and the parent verifies every exact observation before recording success. Both current and baseline completion flags are supported by the retained maps and successful child outcomes. This bounded golden-edit proof is not a proof of all possible edits.

| Measurement on the same CI runner | Baseline command | Current command | Current cold |
| --- | ---: | ---: | ---: |
| Prepare / source checkpoint reconstruction | 161.497 s | 219.531 s | 163.268 s |
| Producer subjects evaluated | 717 | 717 | 717 |
| Audit subjects evaluated | 791 | 425 | 791 |
| Audit subjects reused | 0 | 366 | 0 |
| Authored records retained | 0 | 568 | 0 |

The current command spends 31.030419 s on closure checkpoint capture and 28.556202 s on rebind; its final closure phase is 142.150815 s, while cold final closure is 84.553929 s. The effective audit remains 50.002232 s versus cold 50.630896 s despite subject reuse. Certificate construction is 25.502320 s versus cold 26.624891 s. These are nested phase counters and must not all be added as independent phases. The current complete oracle process tree sampled 5,751,046,144 peak RSS bytes and 7,012,261,888 peak private bytes at one-second intervals; these are neither native soak memory nor a leak measurement. This is a measured regression, with no claimed semantic incremental wall-time win.

The unchanged runtime's ordinary verify/install/status qualification from [run 36231482800](https://github.com/agentique-systems/agentique/actions/runs/36231482800) was independently checked through the reviewed helper, including stdout/stderr hashes, accepted profile/publication identities, bundle and installer binding. Source comparison over the relevant language/kernel/modeling/runtime and authority inputs permitted only the previously reviewed exact `cfg(test)` addition in `closed_query_audit.rs`; its eight-test gate passed. The retained kernel archive gate passed fourteen tests. The previous `effective-audit-answer-parity02` receipt is historical source `1b5075dc`, and is not represented as a new parity run for the changed audit implementation. The integration lead must review all applicable final semantic gates before applying a freshness update.

The fail-closed helper exited 0 and emitted [proposal.patch](freshness-proposal-01/proposal.patch) and [proposal.json](freshness-proposal-01/proposal.json). It allows exactly the frozen twenty changed hashes and three new production modules; all other authority/receipt/library/runtime metadata remain unchanged. The accepted Cargo.lock input stays pinned through the existing exact 69-package compatibility proof. The helper checked full source consistency and artifact bytes again before emission. It did not edit the ledger, publish anything, or grant publication authority.

Proof-set path: `verification/native-studio-acceptance-proof-set-01.json`, SHA-256 `d394485f03001b2a3a4f209b7be0ca0d759d4d4fe351f829cbc80dc5b946a97d`. This binds the original downloaded `current/oracle/observations/result.json`. A separately extracted unique final log record was also retained as review evidence and compared equal to the original result; it is not substituted into the proof set. Exact artifact metrics/extraction provenance are retained in [independent review JSON](freshness-oracle-independent-review-01.json), and the helper's actual command, stdout/stderr and exit code in [invocation receipt](freshness-proposal-invocation-01.json).

Before applying anything, repeat source consistency against the proof commit and inspect the exact patch. Any imminent producer/audit performance fix makes this proposal historical evidence only. Preserve the accepted semantic/library/artifact identities and run the required freshness/repository checks after an explicitly reviewed final update.
