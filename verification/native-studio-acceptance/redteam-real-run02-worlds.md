# Real-run02 systems and product review

Reviewed the actual 1600×1000 native gallery at `verification/generated/native-studio-acceptance/real-run02/gallery`, from native executable SHA-256 `fb2fedafc3d2e1574f526d1763cc99521cce64b4e7ad058e29665d616004e30e`. These observations were made while the first process was still constructing its candidate, after 30 passed journey assertions. This document does not declare the complete journey or Alpha accepted.

## Systems engineer perspective

The Requirements view answers the local engineering question coherently. `ImmutableRevisions` connects through its literal `subjectWorkspace` to `ProjectWorkspace`; an independently drawn ownership route leads to `preservesPriorState`. Four nodes are actually present, not three: the owned constraint is meaningful extra detail. Role columns, exact link labels, authored Inspector identity and source link make the view inspectable. Zero satisfaction and verification declarations are stated explicitly, and the view does not imply that modeled links prove either success.

Largest remaining criticism: the reference and constraint are rendered as generic `ELEMENT` cards with `?` explorer icons despite known `ReferenceUsage` and `ConstraintUsage` kinds. An engineer can follow the subject path but must infer what the constraint means from its identifier. Architecture-first requirement inquiry and real Satisfaction/Verification declaration interactions are not exercised by this capture.

Change already substantiated: grouped Requirements lanes, original two-hop relationships, selected-obligation focus and revision-bound Inspector replaced the earlier foundation graph. Remaining weakness is semantic communication of owned detail and the unexercised role coverage. Observed Requirements neighborhood: Alpha-quality for this real scope; no claim of satisfaction evaluation.

System World, the focused ModelingPlatform and selected real port are coherent engineering views. The port Inspector identifies its interface and exact connection while honestly stating that direction is unspecified. Explain presents a concrete evidence→rule→consequence graph and labels its partial support. These scenes support the existing Alpha-quality judgment; they do not resolve startup/candidate latency.

## Product designer perspective

Largest History criticism: `06a-earlier-revision` displays baseline, empty project, then newest AgentFabric change. Reliable timestamps are 09:42, 09:39 and 09:44 respectively. The line traverses cards in this non-chronological order, so its apparent narrative does not follow design evolution. Readable titles and validation/branch metadata help, but source-file summaries remain less informative than authored design-intent summaries.

Largest Diff criticism: `06-history-diff` has useful modes, owner groups and projected-change counts, but its first view shows only 8/130 scene elements and clips the changed area at the right edge. The owner chooser says AgentArchitecture while the Inspector still displays ImmutableRevisions from the earlier Requirements context. That Inspector is revision-correct, but visually unrelated to the new comparison. Entering a durable comparison should show changed-owner context and deliberately select a relevant target or clear unrelated inspection. Candidate review must retain its separate explicit-selection contract.

The integration lead authorized two bounded follow-ups: order History by actual parent lineage with stable branch-aware tie-breaking, and frame the initial durable comparison while avoiding unrelated old-world selection/Inspector context. Recapture is pending; neither defect is recorded as resolved by this initial review. At this point History and initial dense Diff remain Foundation in product coherence.

## Navigation evidence

The requirement-focused `04a` image and restored `06b` image are byte-identical SHA-256 `5ff6c560bb330f7d1f4ee97307497c43d174eab517d3c4a3ee48ff447873ba16`. The report separately records two actual parent visits and successful returns: Requirements, then Graph with selected AgentRuntime. Head `a7f64fce-43b2-4be6-8fb5-69c16f646dc3` remained head; parent `52bc3735-3701-4876-8348-31b7c51827ab` was actually loaded. Requirement overview and focus both contain four nodes, with the exact canonical subject path retained.

The observed focus step was 357 ms, parent visit 296 ms, requirement return 405 ms, and initial parent comparison 1,011 ms. These are whole scripted-step durations including ordinary input cadence and settling; they are not isolated query benchmarks.

Other retained screenshot hashes:

| Capture | SHA-256 |
| --- | --- |
| `04-requirements-world.png` | `a30aa201a9049726252b1f9b3879b758918671f94a576caa6b5b13e1a098fadd` |
| `06a-earlier-revision.png` | `9702d468d07a61844b948556af32a943b48d3d9b8cf61f01804895eb7c806e54` |
| `06-history-diff.png` | `456ee36d9c96bd68d1b018309422f61fc090ad6c3dbf77f289376022058d9d8c` |

These are two reviewer perspectives on the current actual gallery, with concrete remaining fixes. They are not five completed critique/change/recapture rounds.
