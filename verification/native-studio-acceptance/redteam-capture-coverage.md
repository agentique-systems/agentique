# Real product review: capture and observation requirements

This is a review checklist grounded in the current real scenario source, not five completed critique rounds. The current running executable predates the latest interaction-agent assertions described below. Capture names alone do not demonstrate a successful run: retain the executable/source identity, matching scenario report, exact revision/selection/focus data and assertion results beside the images.

## Existing and newly prepared coverage

| Real scene | Existing evidence/check | Latest prepared addition | Still needed for critique |
| --- | --- | --- | --- |
| Requirements overview | `04-requirements-world`; actual Requirements projection and canonical `ImmutableRevisions -> subjectWorkspace -> ProjectWorkspace` path, with both original edges present in the scene. Soak retains `13-soak-requirements`. | `04a-requirement-neighborhood`; select the real obligation and invoke Show requirements affecting selection; exact focus, selection, Inspector revision and two-hop relationship identity. | Readability of all three roles; long-name/text layout; architecture-first inquiry from ProjectWorkspace; honest empty satisfaction/verification state. |
| Earlier revision | Existing History check proves the displayed durable manifest. Parent Diff compared the correct parent but did not itself prove visiting that revision. | `06a-earlier-revision`; click the actual parent card and compare observed manifest/binding/projection with the requested parent while head remains unchanged. | Whether a systems engineer understands the card's change summary, branch lineage and validation state without decoding UUIDs. |
| Return to head | Existing soak Back check compares world, focus, camera, selection and filters; old soak Head check only established head/current mode. | `06b-restored-requirement-exploration`; restore saved Requirements context through Return to head. A second parent visit/return checks the selected changed AgentRuntime Graph context and its camera/filters. | Before/after images with the same meaningful selection and camera; no hidden history reset, unexplained fit, stale Inspector or filter loss. |
| Dense parent Diff | `06-history-diff`; matching projection lenses, exact parent revision, actually added AgentRuntime. Soak retains `14-soak-dense-parent-diff` and checks the added committed part. | Parent visit/return also preserves its prior Graph exploration context. | Structure versus Relationships/Requirements/All, owner-group overview, open change browser, Next/Previous Change and Focus changed owner. Existing scenario does not exercise those new controls. |
| Candidate Diff | `09-candidate-diff`; real added part is marked Added in the exact candidate. Candidate review and validation/commit are independent checks. | Soak retains `15-soak-rename-candidate`, validates then cancels the real rename. | Readable owner grouping around ModelingPlatform, current/candidate revision labels, rename identity continuity, and no claim that a part edit proves requirement satisfaction. |
| History after commit/restart | `10-committed-history`, `12-restarted-history`; the expected durable manifest/head and separate-process canonical part identity are checked. | Earlier-revision checks add actual historical navigation. | Whether cards summarize Create/Rename/import meaningfully; chosen comparison pair and return action remain obvious after restart. |

The interaction agent is implementing the new checks through ordinary native actions. Its new report fields `engineering_evidence.requirement_neighborhood` and `history_visits` retain the expected and observed data. Do not attribute these checks to an older executable merely because their source exists.

## A concrete requirement-scope trap

The real self-model currently declares one requirement, `ImmutableRevisions` at `models/agentique/ModelingPlatform.sysml:54`, and no satisfaction or verification declarations. Requirements layout now excludes the owning package from the obligation neighborhood. Its entire overview can therefore already be the requirement, literal subject and architecture type.

A focused neighborhood must preserve those exact identities and edges and bind the Inspector to the selected revision. It need not have fewer nodes when the overview contains no outside context. A strict `focused_nodes < overview_nodes` check would reject a correct three-node view. This was reported to the interaction agent: require exclusion of actual outside context where present and verify bounded focus independently, rather than forcing a node-count decrease.

The absence of modeled satisfaction/verification is a real modeling limitation. Keep the zero counts visible and the explanation that modeled links do not establish engineering success. Do not add fake edges, success badges or fixture screenshots to make the real gallery appear complete. If broader role interaction must be qualified, retain a separate source-backed acceptance revision with actual supported modeled links and state its scope; it does not replace the unmodified real-self-model gallery.

## Five critique rounds to perform after actual captures exist

| Round/reviewer | Required views and operator observation | Largest question to answer | Record after changes |
| --- | --- | --- | --- |
| Systems engineer | `04`, `04a`, real Inspector; then select ProjectWorkspace and ask for affecting requirements. | Can I identify the obligation, its literal subject and the architecture it concerns, without mistaking a link for satisfaction? | Largest criticism, implemented change, recapture and remaining semantic/product limitation. |
| Graph expert | Uncurated `06`/`14` dense comparison, then Relationships and All, selected incident relation and open change list. | Can I follow one important relationship and one changed owner despite crossings and long labels? | Labels hidden/revealed, route/node/label collisions, remaining ambiguity and same graph/revision recapture. |
| Product designer | `06a`, `10`, `12`, revision-card chooser and selected-pair comparison; include the open browser state. | Does History explain design evolution, and does Diff provide a useful overview before detail? | Card summary/metadata clarity, canvas space, focus hierarchy, repeat capture without selecting only flattering scenes. |
| Editor interaction expert | Before History, `06a`, `06b`, second Graph round trip, plus Next/Previous Change/Focus owner. | Do navigation and review controls preserve the exact world, focus, camera, selection, filters and revision? | Paired screenshots plus saved/observed presentation data; include failures and selection or camera jumps. |
| AI-native workflow expert | `07-agent-view`, `05-explain`, `04a`, `08`/`09` and rename review. | Is provider/intent/input revision/selection/result provenance clear, and can I distinguish a deterministic operation from semantic validation? | Provenance readability, retained current-world exploration, candidate impact honesty and remaining workflow friction. |

Do not call these five rounds complete from one set of source assertions. Each requires an actual reviewed image/operator journey, a recorded criticism, a resulting change or explicit decision, and a remaining weakness. Unit/property tests substantiate interaction invariants but do not replace product judgment.

## Additional captures and machine observations

* Architecture-centric requirements: select the real `ProjectWorkspace` architecture element and invoke the same Requirements command; retain the selected canonical ID, resulting focus/definition and unchanged subject path. The requirement-selected `04a` alone does not cover this direction.
* Diff modes: retain one image each for Structure, Relationships, Requirements and All over the same exact pair. A Requirements mode with no projected changes must say so honestly. Displayed counts are projected changes, not all canonical semantic facts.
* Dense group detail: keep the full-scene overview, focused changed owner and open change browser. Record that all original before/after projections remain available and that removed-object Inspector binds to the old revision.
* Change navigation: record successive canonical targets and their own revisions while cycling Next/Previous and focusing the owner. Record a selected edge that is revealed despite lower-priority mode filtering.
* History cards: include branch labels, validation state and actual available metadata at readable scale. Time and descriptions must come from retained reliable metadata; no invented revision numbers or semantic summaries.
* Inspector labels: retain a real long qualified name with its collapsed/wrapped state and tooltip/copy affordance. Pair this with existing bounded-width unit evidence; a short real name does not qualify extreme label handling by itself.
* Every captured scene must retain current/candidate/earlier revision context. Capture failures, sparse/no-result scenes and dense crossings alongside successful focused scenes. A screenshot cannot by itself establish source-only recovery, a durable commit, semantic speedup or absence of stale asynchronous replies.

No native application, rendering run or semantic build was launched for this checklist. It records code coverage and the remaining concrete review evidence only.
