[package]
name = "agq-kerml"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Normative KerML Root/Core descriptors and borrowed typed semantic views"

[dependencies]
agq-kernel = { path = "../kernel" }
