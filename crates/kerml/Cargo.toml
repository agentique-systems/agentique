[package]
name = "agq-kerml"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Complete normative KerML 1.0 descriptors and borrowed typed semantic views"

[dependencies]
agq-kernel = { path = "../kernel" }
